#include<bits/stdc++.h>
using namespace std;
#define fastio() ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
int main(){
    int t;
    cin>>t;
    while(t--){
        int A,B;
        cin>>A>>B;
        int ra = ((A%10)*10) + (A/10);
        int rb = ((B%10)*10) + (B/10);
        if(A>B || A>rb || ra>B || ra > rb){
            cout<<"Yes"<<'\n';
        }
        else{
            cout<<"No"<<'\n';
        }
    }

}
